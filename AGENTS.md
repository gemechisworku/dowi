# Architecture rules

- Keep swipe-action backplates hidden at rest and cover them with an opaque row surface, because translucent list rows otherwise tint every item with the destructive color.
- Keep standard page titles, section tabs, and immediate filters inside one shared brand-gradient band so every product area retains the same hierarchy.